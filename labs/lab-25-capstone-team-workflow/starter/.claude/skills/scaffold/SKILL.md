---
name: scaffold
# TODO: fix this frontmatter so validate_skill_frontmatter() returns []:
#   - add a non-empty `description`
#   - set `context: fork` (isolate verbose output from the main session)
#   - set `allowed-tools` to a non-empty list (e.g. Read, Write, Glob)
#   - set a non-empty `argument-hint`
#   - remove any key that is not one of:
#       name, description, context, allowed-tools, argument-hint
context: forked
tools: Read, Write
---

# scaffold

<!-- TODO: describe what the skill does and why it runs forked. -->

## Steps

<!-- TODO -->

## Output (return only this to the main session)

<!-- TODO -->
