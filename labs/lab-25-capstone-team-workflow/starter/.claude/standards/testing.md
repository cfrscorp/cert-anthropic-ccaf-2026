# Testing standards (imported module)

This focused standards file is pulled into `.claude/CLAUDE.md` via
`@standards/testing.md`. It already exists so your import can resolve — flesh out
the rules to match your team.

- Every module has a matching test.
- Tests must be deterministic and run offline by default.
<!-- TODO: add the rest of your team's testing standards -->
