---
# TODO: add a `description` (required so this renders as a slash command).
# Optionally add `argument-hint` and `allowed-tools`.
---

# /review — team code review

<!--
TODO: Write the team's code-review checklist. This command lives in
.claude/commands/ so it is version-controlled and reaches every developer on
clone/pull (Sample Question 4) — do NOT move it to ~/.claude/commands/.

Cover: correctness, security, the path-scoped rules in .claude/rules/, and test
coverage. Add a cross-file integration pass and a clear output format.
-->

$ARGUMENTS
