---
name: testing
description: Conventions for all test files, wherever they live in the tree.
# TODO: add paths globs that match test files ANYWHERE in the tree, e.g.
#   **/*.test.tsx and **/*.test.ts  (Button.test.tsx AND src/ui/Button.test.tsx)
paths: []
---

# Testing conventions

<!-- TODO: document the testing conventions. Note that a glob rule is more
     maintainable than a per-directory CLAUDE.md because test files are scattered
     next to the code they cover (Sample Question 6). -->
