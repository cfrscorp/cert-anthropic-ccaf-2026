---
name: api
description: Conventions for HTTP API handlers under src/api.
# TODO: add a paths glob so this rule loads only when editing API handlers.
# It must match src/api/users.ts AND src/api/v1/handlers.ts.
paths: []
---

# API handler conventions

<!-- TODO: document the API conventions (async/await, schema validation,
     structured error responses, no secrets in logs). -->
