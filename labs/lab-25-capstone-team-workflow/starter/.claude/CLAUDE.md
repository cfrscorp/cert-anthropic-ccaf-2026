# Team Project — Project Memory (project-level)

<!--
TODO: This project-level CLAUDE.md is committed to version control, so every
teammate gets it automatically. Fill it in:

1. Write a short set of UNIVERSAL coding standards (type hints, pure functions,
   never commit secrets, ...). These are always loaded on every turn.
2. Add an @import that pulls in the shared testing standards from
   standards/testing.md (the file already exists next to this one). The import
   line looks like:  @standards/testing.md
   validate_project() checks that every @import resolves to a real file.

Anything that should apply only to certain file types belongs in a path-scoped
.claude/rules/*.md file instead; anything personal belongs in your own
~/.claude/CLAUDE.md (not shared with teammates).
-->

## Universal coding standards

<!-- TODO: list the universal standards here -->

## Testing

<!-- TODO: add the @import to standards/testing.md here -->
