# Broken project config (fixture)

This fixture is deliberately misconfigured so `validate_project()` reports
problems. The `@import` below points at a file that does not exist.

@standards/missing.md
