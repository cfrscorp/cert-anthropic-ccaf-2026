# MCP resource: content catalog example

TODO: Document ONE MCP *resource* that exposes a content catalog (e.g. issue
summaries, a documentation hierarchy, or a database schema) to reduce
exploratory tool calls.

Your write-up should:

- Contrast a **resource** (addressable content the agent *reads*) with a
  **tool** (an action the agent *invokes*).
- Give a concrete catalog example with resource **URIs** and a sample payload.
- Explain **why** publishing the catalog as a resource reduces exploratory tool
  calls compared with discovering the same data through repeated tool calls.

Delete this TODO block when done.
