{
  "_note": "Illustrative excerpt of a USER-scoped config (~/.claude.json). It is NOT committed to the repo; it applies only to this developer and is the right home for personal/experimental servers that teammates should not inherit.",
  "mcpServers": {
    "sqlite-scratch": {
      "command": "uvx",
      "args": ["mcp-server-sqlite", "--db-path", "/tmp/scratch.db"]
    }
  }
}
