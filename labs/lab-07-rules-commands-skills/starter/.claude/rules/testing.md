---
name: testing
description: Conventions for all test files, wherever they live in the tree.
# TODO: add a `paths:` list of globs so this rule loads ONLY when editing test
# files, no matter which directory they live in. Test files look like
# Button.test.tsx and helpers.test.ts and are scattered across the tree.
# paths:
#   - "**/*.???"
---

# Testing conventions

TODO: document the team's test conventions here (test runner, naming,
Arrange–Act–Assert, no real network/timers, etc.).
