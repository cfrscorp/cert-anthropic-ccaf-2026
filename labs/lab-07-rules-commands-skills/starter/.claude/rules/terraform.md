---
name: terraform
description: Conventions for Terraform infrastructure code.
# TODO: add a `paths:` glob that matches every file under terraform/ (any depth).
# paths:
#   - "terraform/???"
---

# Terraform conventions

TODO: document the Terraform conventions (module layout, typed variables,
pinned provider versions, no hardcoded secrets, etc.).
