---
name: api
description: Conventions for HTTP API handlers under src/api.
# TODO: add a `paths:` glob that matches every file under src/api (at any depth).
# paths:
#   - "src/api/???"
---

# API handler conventions

TODO: document the API handler conventions (async/await, request validation,
structured error responses, etc.).
